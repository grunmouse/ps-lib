%математические функции

/sq {
	dup mul
} bind def

/sign {
	dup
	0 ne
	{
		0 le
		{-1}{1}
		ifelse
	}
	{0}
	ifelse
} bind def

%упорядочивает пару числе в стеке так, чтобы в вершине оказалось большее из них
/min {
	2 copy
	%[a, c, a, c]
	gt %a>c
	{
		exch
	}
	if
} def